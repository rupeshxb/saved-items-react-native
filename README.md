# saved-items-react-native
A production-style React Native (Expo) implementation of a Saved Items feature using TypeScript and Firebase (Firestore + Auth). Designed with clear separation of concerns, offline-first data handling, deep linking, and scalability in mind.
